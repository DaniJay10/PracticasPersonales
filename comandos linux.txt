tar -czf pb_data.tar.gz pb_data

# Reemplazamos el snapshot en el repo del backend y hacer push

cd ~/pb-backend
cp ~/pb_data.tar.gz ./pb_data.tar.gz
git add pb_data.tar.gz
git commit -m "update: PocketBase snapshot (esquema/reglas/datos actualizados)"
git push origin main